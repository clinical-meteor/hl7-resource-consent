import { HTTP } from 'meteor/http';
import { Meteor } from 'meteor/meteor';

import { Consents } from '../lib/Consents';

Consents.after.insert(function (userId, doc) {

  
});
Consents.after.update(function (userId, doc) {

});
Consents.after.remove(function (userId, doc) {

});
